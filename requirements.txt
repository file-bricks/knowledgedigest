# KnowledgeDigest v0.4.0
# Benötigt: Python >= 3.10 mit sqlite3 FTS5-Support

# Text-Extraktion (optional, aber empfohlen)
pdfplumber>=0.10.0
PyPDF2>=3.0.0
python-docx>=1.0.0
beautifulsoup4>=4.12.0

# GUI (optional, nur für --gui)
PySide6>=6.6.0
PyMuPDF>=1.24.0

# LLM Summarization (optional, nur für summarize-Befehl)
anthropic>=0.79.0

# Gemini Flash Summarization (optional, nur für summarize --flash)
google-genai>=1.0.0
